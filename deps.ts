import { assert,equal,assertEquals } from "https://deno.land/std@0.104.0/testing/asserts.ts";

export {
    equal,
    assert,
    assertEquals,
}
const o = {attr1: "something", attr2: "something else"}